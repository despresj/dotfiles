- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc: zshrc 
     ~/.gitconfig: gitconfig
     ~/.vimrc: vimrc
     ~/.config/karabiner/karabiner.json: karabiner 
- create:
    - ~/downloads

- shell:
# - [git submodule update --init --recursive, Installing submodules]
  - echo "hey guys"
  - 
    command: ./test.zsh
    stdout: true
    stderr: true
